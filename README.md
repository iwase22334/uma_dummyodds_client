# uma_dummyodds_client

## Install
```
pip3 install .
```

## class DummyRace
* odds_tansyo(id)
  * id: race id
  ```
    [ 'year', 'monthday', 'jyocd', 'kaiji', 'nichiji', 'racenum']
  ```
  * odds_list: list of tuple 
  ```
    [("<umaban>", <odds>), ...] 
  ```
